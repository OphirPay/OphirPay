{
  "name": "ophirpay",
  "version": "0.1.0",
  "private": true,
  "description": "O",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "npm run test:e2e",
    "test:watch": "npm run test:e2e -- --watch",
    "test:openapi": "npm run test:e2e -- --grep @openapi",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:a11y": "playwright test --project=chromium --grep @a11y",
    // ---- Visual regression test scripts (new) ----
    "test:visual": "playwright test -c playwright.visual.config.ts",
    "test:visual:update": "playwright test -c playwright.visual.config.ts --update-snapshots"
  },
  "dependencies": {
    // ... existing dependencies ...
  },
  "devDependencies": {
    "playwright": "^1.45.0",
    // ... existing devDependencies ...
  }
}
